# Base URL of the backend API as seen from the browser. In dev, Vite proxies /api to the backend.
VITE_API_BASE_URL=/api/v1
# Optional label shown in the top bar environment badge (dev | test | prod)
VITE_APP_ENV=dev
